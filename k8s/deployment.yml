apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: dev
  name: game-config-depl
  labels:
    app: game-config-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: game-config-depl
  template:
    metadata:
      labels:
        app: game-config-depl
    spec:
      containers:
      - name: game-config-depl
        image: https://registry.digitalocean.com/digital-dice/game-config:3f1d61f
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: ENVIRONMENT
          value: "dev"
        - name: DB_HOST
          value: "postgresql-postgresql-ha-pgpool"
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgresql-postgresql-ha-postgresql
              key: password
        - name: DB_NAME
          value: gamedb
        - name: REDIS_HOST
          value: "kubernetes.docker.internal"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
           path: /health
           port: 8000
          initialDelaySeconds: 5
          timeoutSeconds: 2
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
           path: /health
           port: 8000
           scheme: HTTP
          initialDelaySeconds: 5
          timeoutSeconds: 2
          periodSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 512Mi